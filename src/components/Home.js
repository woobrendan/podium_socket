import { Card } from "@mui/material";

const home = () => {
    return (
        <section id="homepage">
            <div className="nav_list">
                <Card></Card>
            </div>
        </section>
    );
};

export default home;
